# 🩺 MERN Doctor Appointment System

**Run the app in 3 simple steps:**  
1. Frontend: `cd frontend && npm install && npm run dev`  
2. Backend: `cd backend && npm install && node serve.js`  
3. Open [http://localhost:5173](http://localhost:5173) → Register/Login → Enjoy your dashboard 🎉
